/* JS dependencies */
import React from "react";

/* Stylesheet dependencies */

const Actions = React.createClass({
  render () {
    return (
      <div>Actions</div>
    );
  }
});

export default Actions;